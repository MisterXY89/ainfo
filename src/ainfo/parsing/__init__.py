def parse_data(raw: str) -> dict:
    """Parse the raw HTML into a structured format.

    Parameters
    ----------
    raw: str
        The raw HTML string.

    Returns
    -------
    dict
        A dictionary representing parsed content.
    """
    return {"raw": raw}
